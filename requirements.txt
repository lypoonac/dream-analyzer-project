streamlit==1.38.0
transformers==4.44.2
torch  # No exact version - lets Streamlit install a compatible one (e.g., latest CPU version)
# datasets==2.21.0  # Commented out - only needed for fine-tuning, not app runtime; remove to avoid conflicts
