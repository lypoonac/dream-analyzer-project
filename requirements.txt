streamlit==1.38.0
transformers==4.44.2
torch==2.0.1  # Compatible with 3.10, CPU-only for Streamlit
datasets==2.21.0  # For fine-tuning (optional for runtime)
pillow==10.0.0  # Older version to avoid build errors
